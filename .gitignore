/bin/
/aqt
/aqt-server
*.exe
/aqt-data/

# Per-folder sync state (resource id + last-synced manifest with chunk keys);
# never belongs in a repo.
.aqt/

# Local scratch for manually exercising the CLI.
/test/

# Local agent working files; never repo content.
/agents.md
/.claude/
