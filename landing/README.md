# aqt landing

Independent Next.js marketing site for aqt, built with the App Router, Tailwind CSS 4, and GSAP.

```bash
pnpm install
pnpm dev
```

Open [http://localhost:3000](http://localhost:3000).

Quality checks:

```bash
pnpm lint
pnpm typecheck
pnpm build
```
